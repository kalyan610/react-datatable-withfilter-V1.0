"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var isUndefined = function (value) { return typeof value === 'undefined'; };
/**
 * Returns a stateful value, and a function to update it. Mimics the `useState()` React Hook
 * signature.
 */
exports.useAutoControlled = function (options) {
    var defaultValue = options.defaultValue, _a = options.initialValue, initialValue = _a === void 0 ? undefined : _a, value = options.value;
    var _b = React.useState(isUndefined(defaultValue) ? initialValue : defaultValue), state = _b[0], setState = _b[1];
    return [isUndefined(value) ? state : value, setState];
};
