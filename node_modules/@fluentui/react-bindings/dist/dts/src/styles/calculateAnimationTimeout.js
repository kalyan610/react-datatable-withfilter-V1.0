"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var convertCssTimeToNumber_1 = require("./convertCssTimeToNumber");
var _ = require("lodash");
exports.calculateAnimationTimeout = function (duration, delay) {
    if (_.isNil(duration) || duration === 'inherit' || duration === 'initial')
        return 0;
    if (delay === 'inherit' || delay === 'initial')
        return 0;
    return convertCssTimeToNumber_1.convertCssTimeToNumber(duration) + convertCssTimeToNumber_1.convertCssTimeToNumber(delay);
};
