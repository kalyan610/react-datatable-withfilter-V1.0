"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var types_1 = require("../../focusZone/types");
var keyboard_key_1 = require("@fluentui/keyboard-key");
/**
 * @description
 * Provides navigation between focusable children of Grid component with arrow keys in 4 directions.
 * Provides navigation inside the inner focus zone using Enter key
 *
 * @specification
 * Provides arrow key navigation in bidirectional direction.
 * Focus can be moved inside a child component with embeded inner FocusZone by pressing a specified key.
 */
exports.cardsContainerBehavior = function () { return ({
    attributes: {},
    focusZone: {
        props: {
            shouldEnterInnerZone: function (event) { return keyboard_key_1.getCode(event) === keyboard_key_1.keyboardKey.Enter; },
            direction: types_1.FocusZoneDirection.bidirectional,
        },
    },
}); };
