"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var _ = require("lodash");
var treeTitleBehavior_1 = require("./treeTitleBehavior");
/**
 * @description
 * Adds role 'treeitem' if the title is a leaf node inside the tree.
 */
exports.treeTitleAsListItemTitleBehavior = function (props) {
    var behavior = treeTitleBehavior_1.treeTitleBehavior(props);
    var definition = _.merge(behavior, {
        attributes: {
            root: tslib_1.__assign({}, (!props.hasSubtree && {
                role: 'listitem',
            })),
        },
    });
    if (process.env.NODE_ENV !== 'production' && props.hasSubtree) {
        // Override the default trigger's accessibility schema class.
        definition.attributes.root['data-aa-class'] = 'TreeTitleList';
    }
    return definition;
};
