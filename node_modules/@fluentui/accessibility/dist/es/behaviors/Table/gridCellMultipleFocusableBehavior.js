import { IS_FOCUSABLE_ATTRIBUTE } from '../../attributes';
import { FocusZoneDirection } from '../../focusZone/types';
import { keyboardKey } from '@fluentui/keyboard-key';
/**
 * @specification
 * Adds role='gridcell'.
 * Adds attribute 'data-is-focusable=true' to 'root' slot.
 * Provides arrow key navigation in bidirectional direction.
 * Triggers 'focusCell' action with 'Escape' on 'root'.
 */

export var gridCellMultipleFocusableBehavior = function gridCellMultipleFocusableBehavior(props) {
  var _root;

  return {
    attributes: {
      root: (_root = {
        role: 'gridcell'
      }, _root[IS_FOCUSABLE_ATTRIBUTE] = true, _root)
    },
    focusZone: {
      props: {
        direction: FocusZoneDirection.bidirectional
      }
    },
    keyActions: {
      root: {
        focusCell: {
          keyCombinations: [{
            keyCode: keyboardKey.Escape
          }]
        }
      }
    }
  };
};
//# sourceMappingURL=gridCellMultipleFocusableBehavior.js.map
