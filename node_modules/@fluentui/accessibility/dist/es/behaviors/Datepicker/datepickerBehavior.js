import { keyboardKey } from '@fluentui/keyboard-key';
/**
 * @description
 * Behavior for a datepicker component
 * @specification
 * Triggers 'open' action with 'Enter' on 'input'.
 */

export var datepickerBehavior = function datepickerBehavior(props) {
  return {
    keyActions: {
      input: {
        open: {
          keyCombinations: [{
            keyCode: keyboardKey.Enter
          }]
        }
      }
    }
  };
};
//# sourceMappingURL=datepickerBehavior.js.map
