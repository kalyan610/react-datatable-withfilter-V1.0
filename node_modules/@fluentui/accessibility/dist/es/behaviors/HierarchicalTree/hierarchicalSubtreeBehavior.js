import { keyboardKey } from '@fluentui/keyboard-key';

/**
 * @specification
 * Triggers 'expandSiblings' action with '*' on 'root'.
 * Adds role 'group' to 'root' slot.
 */
export var hierarchicalSubtreeBehavior = function hierarchicalSubtreeBehavior() {
  return {
    attributes: {
      root: {
        role: 'group'
      }
    },
    keyActions: {
      root: {
        expandSiblings: {
          keyCombinations: [{
            keyCode: keyboardKey['*']
          }]
        }
      }
    }
  };
};
//# sourceMappingURL=hierarchicalSubtreeBehavior.js.map
