"use strict";

exports.__esModule = true;
exports.dropdownSelectedItemBehavior = void 0;

/**
 * @description
 * Add tabIndex to 0 if active and -1 if not
 * Adds `aria-label` for remove icon
 * @specification
 * Adds attribute 'aria-selected=true' to 'root' slot.
 */
var dropdownSelectedItemBehavior = function dropdownSelectedItemBehavior(props) {
  return {
    attributes: {
      root: {
        'aria-selected': true,
        tabIndex: props.active ? 0 : -1
      },
      icon: {
        'aria-label': "Remove " + props.header + " from selection."
      }
    }
  };
};

exports.dropdownSelectedItemBehavior = dropdownSelectedItemBehavior;
//# sourceMappingURL=dropdownSelectedItemBehavior.js.map
