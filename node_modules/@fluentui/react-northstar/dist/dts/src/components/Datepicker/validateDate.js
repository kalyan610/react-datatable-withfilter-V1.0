"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var date_time_utilities_1 = require("../../utils/date-time-utilities");
exports.validateDate = function (futureSelectedDate, futureFormattedDate, calendarOptions, dateFormatting, required) {
    if (futureSelectedDate && !isNaN(futureSelectedDate.getTime())) {
        if (date_time_utilities_1.isRestrictedDate(futureSelectedDate, calendarOptions)) {
            return dateFormatting.isOutOfBoundsErrorMessage;
        }
    }
    else if (futureFormattedDate) {
        return dateFormatting.invalidInputErrorMessage;
    }
    else if (required && !futureSelectedDate) {
        return dateFormatting.isRequiredErrorMessage;
    }
    return '';
};
