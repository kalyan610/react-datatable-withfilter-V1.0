"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var _ = require("lodash");
var FluentUI = require("src/index");
// ----------------------------------------
// Is exported or private
// ----------------------------------------
exports.isExportedAtTopLevel = function (constructorName, displayName) {
    var isTopLevelAPIProp = _.has(FluentUI, constructorName);
    // require all components to be exported at the top level
    test('is exported at the top level', function () {
        var message = ["'" + displayName + "' must be exported at top level.", "Export it in 'src/index.js'."].join(' ');
        expect({ isTopLevelAPIProp: isTopLevelAPIProp, message: message }).toEqual({
            message: message,
            isTopLevelAPIProp: true,
        });
    });
};
