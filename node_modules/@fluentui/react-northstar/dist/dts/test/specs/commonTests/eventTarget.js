"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EVENT_TARGET_ATTRIBUTE = 'data-simulate-event-here';
exports.getEventTargetComponent = function (wrapper, listenerName, eventTargets) {
    if (eventTargets === void 0) { eventTargets = {}; }
    var eventTarget = eventTargets[listenerName]
        ? wrapper
            .find(eventTargets[listenerName])
            .hostNodes()
            .first()
        : wrapper
            .find("[" + exports.EVENT_TARGET_ATTRIBUTE + "]")
            .hostNodes()
            .first();
    // if (eventTarget.length === 0) {
    //   throw new Error(
    //     'The event prop was not delegated to the children. You probably ' +
    //     'forgot to use `getUnhandledProps` util to spread the `unhandledProps` props.',
    //   )
    // }
    return eventTarget;
};
