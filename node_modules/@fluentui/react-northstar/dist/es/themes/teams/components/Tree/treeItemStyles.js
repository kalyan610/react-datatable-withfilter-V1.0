import { pxToRem } from '../../../../utils';
import { getBorderFocusStyles } from '../../getBorderFocusStyles';
import { treeTitleClassName } from '../../../../components/Tree/TreeTitle';
export var treeItemStyles = {
  root: function root(_ref) {
    var _focusVisible;

    var siteVariables = _ref.theme.siteVariables,
        p = _ref.props;
    return Object.assign({
      listStyleType: 'none',
      padding: "0 0 0 " + pxToRem(1 + (p.level - 1) * 10)
    }, getBorderFocusStyles({
      variables: siteVariables
    })[':focus'], {
      ':focus-visible': (_focusVisible = {
        outline: 0
      }, _focusVisible["> ." + treeTitleClassName] = Object.assign({
        position: 'relative'
      }, getBorderFocusStyles({
        variables: siteVariables
      })[':focus-visible']), _focusVisible)
    });
  }
};
//# sourceMappingURL=treeItemStyles.js.map
